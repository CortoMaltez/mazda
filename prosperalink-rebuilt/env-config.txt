# Configuration des variables d'environnement pour ProsperaLink
# Copiez ce contenu dans un fichier .env.local à la racine du projet

# Base de données
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Stripe (optionnel pour le développement)
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key_here"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_here"
STRIPE_PUBLISHABLE_KEY="pk_test_your_stripe_publishable_key_here"

# Instructions :
# 1. Créez un fichier .env.local à la racine du projet
# 2. Copiez le contenu ci-dessus dans ce fichier
# 3. Remplacez les valeurs par vos propres clés
# 4. Pour Stripe, créez un compte sur https://stripe.com et récupérez vos clés de test
# 5. Pour NEXTAUTH_SECRET, générez une clé aléatoire (ex: openssl rand -base64 32) 